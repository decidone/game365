doctype html
//- /main/login
html
    head
        link(rel="stylesheet" href="./stylesheet/layout.css" type="text/css")
        meta(charset='utf-8')
        title 로그인
    body
        div(id="title")
            div(class="title") 
                a(href= '/main') GAME365
        //-
            클릭시 home.pug로 이동
            /login_process에서 검사 후 /main으로 redirect
        form(action='/main/login/process', method='post')
            div 아이디
                input(type='text' name='id' placeholder='아이디를 입력해 주세요')
            div 비밀번호
                input(type='password' name='password')
            div
                input(type="submit" value='로그인')